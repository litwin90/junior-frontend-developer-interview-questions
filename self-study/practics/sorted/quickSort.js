// Быстрая сортировка
const quickSort = (array) => {
    // if (!array || array.length === 0) { return array; }
    // const first = array.shift();
    // const left = array.filter(x => first > x);
    // const right = array.filter(x => first <= x);
    // return [].concat(quickSort(left), [first], quickSort(right));
}

module.exports = quickSort;