// Очередь
// FIFO
class Queue {
    constructor(items = []) {
        // this.items = items;
    }

    add(item) {
        // return this.items.push(item);
    }

    remove() {
        //  return this.items.shift();
    }
}

module.exports = Queue;
